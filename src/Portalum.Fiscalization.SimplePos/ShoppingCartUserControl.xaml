<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Portalum.Fiscalization.SimplePos"
             xmlns:Services="clr-namespace:Portalum.Fiscalization.SimplePos.Services" x:Class="Portalum.Fiscalization.SimplePos.ShoppingCartUserControl"
             mc:Ignorable="d" 
             d:DesignHeight="453.245" d:DesignWidth="316.466">
    <Grid>
        <ItemsControl ItemsSource="{Binding}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Label Content="{Binding}"
                        Padding="10"
                        Background="#607686"
                        Foreground="White"
                        BorderThickness="0,0,0,1"
                        BorderBrush="white"
                        FontWeight="Bold"
                      />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Button Content="Bezahlen" Margin="10,0,10,13" RenderTransformOrigin="-0.032,-1.82" Height="35" VerticalAlignment="Bottom" Click="Button_Click" FontSize="20"/>
    </Grid>
</UserControl>
